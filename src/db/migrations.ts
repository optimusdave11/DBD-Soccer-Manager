export function runMigrations(version: number): void {
    switch (version) {
        case 1:
              // Initial database
                    break;

                        default:
                              break;
                                }
                                }
}